# 作業履歴・AI指示の見直し（2026-09-05）

これは監査結果と運用手順であり、機能実装のExecPlanではない。

## 調査範囲と限界

現在の `AGENTS.md`、`PLANS.md`、ローカルのSuperpowers手順、OpenAI Docs手順を確認した。リポジトリ内の隠しファイルを含む検索ではAGENTS.mdはルートのみ、CLAUDE.mdは見つからなかった。ユーザー共通の `C:/Users/Hodaka/.codex/AGENTS.md` は0バイトだった。注入されたアプリ側指示はこの空ファイルとは別物であり、ここを書き換えても整理できない。

過去作業はメモリ登録の7作業群を確認し、天気研究とAutoYoutubeの詳細要約2件、会話「Hyperliquid接続を確認」（01a02881-ccd9-70a3-9556-c4c322950147）の直近3ターンを追加確認した。全会話の網羅的集計ではない。過去の実行状態は現在の状態を示さない。

Computer History相当の履歴取得ツールは公開ツール一覧に見つからなかった。`C:/Users/Hodaka/.codex/computer-use/` はconfig.jsonのみだった。PC全体に履歴が存在しないと断定はできず、人が毎回行う操作の頻度は未確認。以下はAI作業ログに見える反復候補である。

## 観察と改善候補

| 観察・根拠 | 手順化・自動化候補 | 優先度・注意 |
| --- | --- | --- |
| 天気・Base LP研究で成果物の正常性とデータ不足の区別が必要だった | 収集前後に要求期間、実測期間、日付重複、欠測、評価状態を検証する | 高。既存validatorを利用し、不足時は追加バックテストより取得条件を確認 |
| Hyperliquidの直近2回の状況確認でSQL集計とPID・口座確認を反復 | 読み取り専用のstatusコマンドへ集約。run ID、プロセス、最終更新、エラー集計、残存建玉を出力 | 高。既存reportはあるが稼働状態と異なる。取引やcleanupの自動実行は含めない |
| AutoYoutubeで9件中6件が同じスキーマ制限で失敗 | runner前に全入力をvalidateし、成功記録を除外して失敗だけ再実行 | 高。6件は1バッチ内の同種不具合であり、6回の別依頼とは数えない |
| 天気研究でプロキシ接続失敗、Hyperliquid会話でもプロキシ調整 | 接続前診断に環境と接続先の判定を追加する候補 | 中。過去の回避策を全通信へ一律適用しない |
| BTC研究・AutoYoutubeでpushやアップロード後の照合 | 依頼された外部反映のID・件数・remote SHAを確認して完了報告に添える | 中。再アップロードやforce-pushで照合を代用しない |

自動化候補は今回実装していない。既存のhyperliquid-ai-traderには開始時点からコード・テスト・報告の変更があるため、今回の指示整理と混ぜず、具体的な機能変更時に差分と設計を確認する。

## 指示の採否

| 対象 | 判断 | 理由・今回の扱い |
| --- | --- | --- |
| AGENTS.mdの日本語ExecPlan・.agent配置 | 残す・短く整理 | リポジトリ固有の必要事項。複雑な機能・大規模変更に限定する元の意図を維持 |
| using-superpowersの「1% chance」「BEFORE any response」 | 適用条件を狭める | 無関係なSkill読込へ連鎖する。現行の実用性に基づくSkill選択と衝突。リポジトリ方針で機械的適用を除外 |
| brainstormingの「EVERY project」、設計と文書の二重承認 | 小変更では不要 | 調査・文書修正で停止する原因。承認済みの範囲で継続する方針を明記 |
| writing-plansとPLANS.mdの別々の計画形式 | ExecPlanへ一本化 | 過去の天気研究では設計文書とExecPlanを別作成。内容が異なる設計資料は保持し、同内容の二重計画だけ省く |
| test-driven-developmentの一律適用と例外の承認要求 | 挙動と回帰リスクに応じる | 実装検証は残す。文書修正へのテスト追加や、依頼済み例外の再確認を避ける |
| verification-before-completionの証拠確認 | 残す | 過去のデータ不足・未完了cleanup・外部反映で必要性がある |
| PLANS.mdの「commit frequently」 | 今回は変更しない | 大規模実装用の文書。小作業にPLANS.md全体を適用しないことで負担を抑える |
| キャッシュ配下・ユーザー共通Skills | 今回は直接編集しない | 他リポジトリにも影響する。今回の改善はこのリポジトリに適用し、原本の具体的な縮小候補を本表に残す |

参照したSkill原本は `C:/Users/Hodaka/.agents/skills/superpowers/` 以下の using-superpowers、brainstorming、test-driven-development、writing-plans、verification-before-completion の各SKILL.md。以前のモデル対策として導入されたかは履歴から確定できないため、年齢やモデル名ではなく現在の衝突と必要性で判断した。

公式資料も、SkillsとAGENTS.mdの矛盾を監査し、ユーザー指示との優先順位を明確にすることを勧めている：[OpenAI model guidance](https://developers.openai.com/api/docs/guides/latest-model)。

## 再利用する短い作業手順

着手時に対象ディレクトリ、既存差分、READMEまたはrunnerを確認する。要求された完了範囲を定め、実装・収集・外部反映を混同しない。障害時は失敗項目と一次ログを特定し、環境障害、入力不正、データ不足、実装不具合を分ける。最小修正後は失敗分を再実行し、必要な既存検証を行う。結果には実行IDまたは成果物、検証状態、残る制約を記す。

## 同じタスクによる比較

今回実施したのは以下の静的な指示比較であり、独立したモデル実行のA/B試験ではない。速度・token・品質の実測改善は未検証。

| 同一入力 | 変更前に残る曖昧さ | 変更後の期待される判断 |
| --- | --- | --- |
| READMEの誤字を直して | Skillsの全作業設計承認が適用され得る | 計画書・追加テストなしで修正し差分確認 |
| 収集済みデータで要求7日間のバックテスト結果を報告して。実データは3.6時間 | AGENTS.mdにデータ充足の判断がない | 部分期間と不足を報告し、7日間の性能評価としない |
| 成功3件・失敗6件のバッチを直して再実行して | AGENTS.mdに成功分の扱いがない | 成功記録を照合し失敗6件のみ再実行 |
| run-localを起動して。終了まで待たなくてよい | 一般的な完遂指示と待機不要が衝突し得る | 起動と初期ログを確認して終了 |
| 複数モジュールを変更する研究機能を追加して | 複数Skillの計画書が重複し得る | 日本語ExecPlanを.agentに作成して更新 |

実測する場合は変更前のAGENTS.md（GitのHEAD版）と変更後を、同じモデル・推論設定・入力・ツール・リポジトリ状態で別セッションに適用する。他のSkills・アプリ指示は固定し、各ケースを3回ずつ実行して、不要な承認回数、計画書数、成功分の再実行、根拠のない成功表現、所要時間、token数を記録する。バッチや起動は副作用のないfixtureを使う。品質基準は誤った成功宣言と重複実行が0件、複雑変更の計画と必要検証を維持すること。時間だけを採用判断にしない。

## 今回の変更と検証範囲

AGENTS.mdを整理し、この監査記録を追加した。共通Skills・メモリ・研究コードは変更していない。文書変更のため差分、参照先、空白エラーを確認し、研究実行・売買・アップロードは行わない。
